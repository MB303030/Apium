name: iOS WDIO Tests (On Demand)

on:
  workflow_dispatch:

jobs:
  ios-test:
    runs-on: macos-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js (LTS)
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      # 🔨 Build the UIKitCatalog app from source
      - name: 🔨 Build UIKitCatalog app
        run: |
          xcodebuild build \
            -project UIKitCatalog/UIKitCatalog.xcodeproj \
            -scheme UIKitCatalog \
            -sdk iphonesimulator \
            -derivedDataPath ./DerivedData \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO

      - name: 📦 Install NPM packages
        run: npm ci

      - name: 📱 List iOS Simulators
        run: xcrun simctl list devices

      - name: 📱 Boot iOS Simulator
        run: |
          DEVICE_NAME="iPhone 16e"   # updated to match runner
          xcrun simctl boot "$DEVICE_NAME" || echo "Simulator already booted"
          # Wait until simulator is fully booted
          until xcrun simctl list devices | grep "$DEVICE_NAME (Booted)"; do
            echo "Waiting for simulator to boot..."
            sleep 5
          done

      - name: 🧪 Run WebdriverIO Tests
        env:
          APP_PATH: ${{ github.workspace }}/DerivedData/Build/Products/Debug-iphonesimulator/UIKitCatalog.app
          IOS_VERSION: '26.2'        # updated to match runner
          DEVICE_NAME: 'iPhone 16e'  # updated to match runner
        run: npx wdio run wdio.ci.conf.js

      - name: 📤 Upload Mochawesome Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mochawesome-reports
          path: reports/mochawesome/
